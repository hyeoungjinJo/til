# 2026-08-20

- 증감 지표에서 상승·하락 동률(tie) 발생 시 정렬 기준을 명시하지 않으면 화면마다 다른 결과가 나올 수 있음. tie-break 규칙을 한 곳에서 통일해야 같은 카테고리가 상승·하락 양쪽에 중복 표시되는 버그도 함께 해결됨
- 모바일 앱과 어드민이 같은 데이터를 각자 계산해 보여줄 때, 두 화면을 나란히 대조하는 QA 페이지를 만들어두면 계산 로직 불일치를 빠르게 검증할 수 있음
- 뱃지 같은 enum 기반 기능은 미사용 값 제거와 신규 값 추가를 함께 정리해 코드·데이터 정합성을 유지
- ISO27001 기술증적 문서는 수기 작성 대신 생성기(AWS 구성도 자동 생성 포함)로 만들면 인프라 변경 시 최신화가 쉬움
- AWS 리소스에 표준 태그 키 세트(7종)를 정의해 일괄 적용하면 비용 배분·리소스 추적·보안 감사 대응이 수월해짐

# 2026-09-04

- 모바일 목록 조회 시 이미지 presigned URL은 필요한 크기(Small 썸네일) 하나만 발급한다. 원본·여러 사이즈 URL을 목록마다 모두 생성하면 서명 연산과 응답 크기가 불필요하게 커진다.
- 클라이언트 로컬에만 있던 즐겨찾기 상태를 서버 저장 API로 옮기면 기기 간 동기화가 가능해진다. 사용자·대상 엔티티 매핑 테이블로 단순 모델링.
- JPA·QueryDSL에서 조건값(id)이 null이면 where 절 자체가 생략되어 fetch join으로 전체 테이블을 메모리에 올리고 heap OOM이 난다. 조회 진입점에서 필수 파라미터 null을 조기 검증해 차단하고, 동적 where 조건에 null이 흘러들어 조건이 사라지는 케이스를 항상 의심할 것.

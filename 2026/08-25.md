# 2026-08-25

- soft delete된 연관 엔티티를 JPA/QueryDSL 집계 쿼리가 그대로 참조해 월간 집계 전체가 실패. 집계 대상 조회 시 삭제 플래그 필터를 조인 조건에 포함시켜 해결. 부분 실패가 전체 실패로 번지는 집계 파이프라인 구조가 원인.
- 플랜 엔티티에 제한 시간(limitTime) 값이 누락된 채 생성되는 경로 보완. 신규 플랜 타입 추가 시 필수 필드 기본값 세팅이 빠지면 하위 로직에서 NPE·무제한 취급으로 번짐.
- 요청 플래그(간편 모드)에 따라 추천 요청 객체를 다르게 조립하고, 기초 테스트 여부로 응답 스펙을 분기. 플래그 기반 분기는 요청 조립 계층에 모아두고 도메인 로직은 단일 경로 유지.
- 외부 본인인증 서비스의 식별키 발급 클라이언트 실연동 구현. 플랫폼 키 주입 + 응답 서명 검증까지 포함. 외부 인증 연동은 mock/live 클라이언트를 인터페이스로 분리해 환경별 주입, 서명 검증은 클라이언트 내부에서 강제.

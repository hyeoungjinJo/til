# 2026-07-28

- Vision 모델 기반 모바일 앱 E2E QA 러너 구축 — 화면 스크린샷을 모델이 판독해 탭 시퀀스를 실행. "슬롯 선택" 모드로 화면당 Vision 호출을 1회로 묶고 경량 모델로 전환해 비용 절감
- 화면 안정 대기를 주기 폴링 대신 화면 해시 비교로 바꾸면 무선 adb 링크 부하가 크게 줄어듦. Wi-Fi 가드도 핑 대신 링크 상태 감시로 교체하고, 끊기면 무선 adb 자동 재연결
- 캐시 교훈 3가지: 빈 레이아웃을 캐싱하면 이후 런 전체가 오염되므로 저장 전 유효성 검증 필요, 스키마가 다른 구버전 캐시 파일은 러너를 죽이지 않게 버전 체크 후 무시, 고정 네비게이션 화면은 탭 좌표 캐시로 탐색 속도 개선
- 에뮬레이터에서 안 보이던 버그(빈 레이아웃 캐싱, 스크롤 한도)가 첫 실기기 런에서 드러남 — 실기기 검증 필수
- 외부 AI 응답 잘림이 원인 불명 502로 표면화되던 문제 — 오류 원인을 구분해 노출해야 디버깅 가능
- input 이벤트 주입이 깨진 기기 대응으로 sendevent 저수준 폴백 마련, 태블릿 등 해상도 차이는 서버 템플릿 좌표 기반 탭 위치 보정으로 흡수
- 장시간 자동화 러너 안정성 패턴: 일시 실패는 재시도, 특정 단계 놓침에는 탈출로를 두어 전체 런이 멈추지 않게 설계. 관찰형·미노출 콘텐츠는 검사 대상에서 제외해 오탐 제거
- 콘텐츠(씬) 구조 정적 검사기 도입 — 런타임 전에 데이터 오류를 잡고, 알려진 이슈 제외 목록으로 노이즈 관리
- AI 프록시에 토큰 사용량을 노출하고 러너에서 실행별 비용 집계 — LLM 기반 자동화는 비용 가시화가 운영의 핵심
- 자동화 커버리지 리포트와 항목별 QA 절차서 자동 생성으로 수동 QA와 자동화 사이 갭 추적
- 민감 데이터 정정 기능은 최상위 관리자 권한으로 제한하고 전용 QA 페이지로 검증
- 이슈 트래커 레이블 오염을 자동 탐지하는 가드 스크립트 추가 — 프로세스 규칙도 코드로 강제

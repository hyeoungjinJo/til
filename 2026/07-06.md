# 2026-07-06

## 백엔드 (Spring Boot / JPA·QueryDSL)
- Spring Boot dev/stg 리소스 라이트사이징(request/limit 조정)
- 독려 알림 1일 1회로 전환

## 추천 서버 (Python / Django)
- gunicorn 워커 타임아웃으로 API가 500 반환하는 문제를 컨테이너 CPU limit 상향으로 대응

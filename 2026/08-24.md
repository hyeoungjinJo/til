# 2026-08-24

- 운영(prd) 프로파일에서 외부 인증 연동 식별키의 MOCK 값 사용을 차단하는 가드를 추가. 환경별 설정 분기만으로는 목 데이터가 운영에 새어 들어갈 수 있으므로, 부팅·요청 시점에 프로파일을 검사해 실패시키는 방어선이 필요하다.
- 외부 인증 기관 식별키를 회원가입·재등록 플로우에 매핑하고, 기기 모델코드 검증을 함께 수행. 최초 등록과 재등록이 같은 식별키 매핑 로직을 공유하도록 정리했다.
- deviceId 기반 자동 로그인 및 기기 변경 시 재등록 API 구현. 기기 교체를 계정 재생성이 아닌 식별자 재바인딩으로 처리해 기존 사용자 데이터를 유지한다.
- 실명 인증 가입 시 상위 계정뿐 아니라 하위 엔티티(Trainee)의 이름 필드까지 실명으로 전환. 인증 결과를 한 엔티티에만 반영하면 연관 엔티티에 가명·임시값이 남아 데이터 정합성이 깨진다.
- 디자인 시안의 등록 플로우와 서버 구현을 정합화: 연락처 유무에 따른 로그인 분기, 주민등록번호 앞자리·학력 필드 반영. 화면 흐름의 분기 조건이 곧 API 분기 조건이므로 시안 기준으로 케이스를 재정의했다.
- 인증 연동 검증용 QA 페이지를 추가해 외부 기관 연동 구간을 수동으로 재현 가능하게 만듦. 외부 의존 플로우는 자동 테스트만으로 커버가 어려워 수동 확인 진입점이 유효하다.

- --
회사·제품·기관명, 티켓 태그 제거함. 프로젝트 1개라 제목 없이 불릿만.

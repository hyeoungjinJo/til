# 2026-09-17

- 문항 종료 API 요청 페이로드에 반응속도·액션 로그를 함께 받아 적재하도록 확장. 클라이언트가 회차 단위로 모아 보낸 행동 데이터를 종료 시점 한 번에 저장하면 진행 중 쓰기 부하를 줄일 수 있다. Spring Boot + JPA 환경에서 로그성 데이터는 별도 엔티티로 분리해 본 도메인 트랜잭션과 결합도를 낮추는 편이 낫다.
- 목록 조회 응답 계약이 클라이언트 실사용 필드를 누락하고 있어 7종을 추가. API 계약 설계 시 서버 도메인 기준이 아니라 클라이언트가 화면에서 실제로 쓰는 필드를 기준으로 잡아야 추가 조회 왕복이 생기지 않는다. QueryDSL projection 으로 필요한 컬럼만 select 하면 필드가 늘어도 N+1 없이 단일 쿼리 유지 가능.

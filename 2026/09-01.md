# 2026-09-01

- 클라이언트가 보낸 `X-Request-Id` 헤더를 수용해 응답에 에코하고, 없으면 서버가 생성하도록 필터를 구성. 요청 ID를 MDC(`rid`)에 넣어 로그 전 구간에서 단일 요청 추적 가능하게 함 — 분산 환경 로그 상관관계(correlation) 확보의 기본 패턴.
- 분류 코드 태깅의 변경 이력을 전체 조회하는 API 추가. 현재 상태만 저장하지 않고 이력 테이블을 분리해 누적하면, 태깅의 시점별 변화를 조회·감사할 수 있음. QueryDSL로 이력 조회 조건을 동적으로 조립.

# 2026-08-12

- 다중 파드(스케일아웃) 환경에서 배치 스케줄러가 인스턴스마다 중복 실행되는 문제 → 분산 락/리더 선출로 단일 실행 보장. 애플리케이션 내장 스케줄러는 단일 인스턴스 가정이 깨지는 순간 집계 중복·데이터 오염으로 이어진다.
- 도메인 코드 체계에서 특정 코드군을 폐기할 때는 코드 정의 삭제만으로 끝나지 않는다. 기존 데이터·통계 집계·화면 매핑에 남은 참조를 함께 정리해야 한다.
- 상태(단계) 전이 로직은 트리거별로 흩어놓지 말고 한 곳에서 재판단하도록 공통화한다. 승인, 설정 변경 등 서로 다른 진입점이 같은 상태를 바꿀 때, 각자 계산하면 조건 하나 추가될 때마다 누락 지점이 생긴다.
- QA·검증용 경로를 운영 경로와 분리해 별도 엔드포인트/페이지로 노출하면, 월간 마감처럼 실행 주기가 길어 재현이 어려운 로직을 실데이터 영향 없이 검증할 수 있다. 모바일 등 클라이언트별 렌더링 결과 확인에도 같은 패턴이 유효하다.
